#include <bits\stdc++.h>
using namespace std;

extern double sigmoid(const double &value);
extern double plain(const double &value);
extern int bitodec(const vector<bool> &seq); 
extern vector<bool> dectobi(const int &num);
extern vector<double> wordFreq(const vector<string> &, const vector<string> &);
extern string forLevel(const double &value);
extern double amply(const double &value);
extern bool sameLayer(const double&, const double&);
